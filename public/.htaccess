Options -MultiViews
RewriteEngine On

# Keep real files and directories available.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Let the client-side TanStack Router handle / and /admin.
RewriteRule ^ index.html [L]
